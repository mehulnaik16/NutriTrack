# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.vercel
.env
node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln

*.sw?

# MCP Configs (contain DB credentials — never commit)
mcp-config.json
.gemini/
.claude/
*.stackdump

# Debug/audit scripts — local only, not needed for production
scratch/

# Architecture docs
architecture.md
apitest.md

# Raw food-data drops. Sources for scripts/build-restaurant-foods.mjs; the
# zip is ~34MB of the scraper's own node_modules. Only the generated
# src/data/restaurantFoods.json belongs in the repo.
/food data.txt
/zenin-nutrition-scraper-main.zip
# Playwright CLI output (may contain credentials)
.playwright-cli/
