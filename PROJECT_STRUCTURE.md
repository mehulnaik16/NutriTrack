# NutriTrack — Project Structure

> Last updated: 2026-07-29 (post ponytail-audit cleanup)

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Framework | TanStack Start (React + SSR, Vite, Nitro) |
| Language | TypeScript |
| Styling | Tailwind CSS v4 |
| UI Primitives | shadcn/ui (14 components) |
| Database | Supabase (PostgreSQL) |
| Auth | Supabase Auth |
| Storage | Supabase Storage (`weight-photos` bucket) |
| AI | Groq API (food recognition, meal suggestions) |
| Barcode | @zxing/browser + jsqr |
| Charts | Recharts |
| Toasts | Sonner |
| Routing | TanStack Router (file-based) |
| Deployment | Vercel (via Nitro `vercel` preset) |

---

## Directory Layout

```
nutritrack/
├── src/
│   ├── components/           # Shared UI components
│   │   ├── BottomNav.tsx     # Mobile bottom navigation bar
│   │   ├── FoodSearch.tsx    # Full food search UI (AI + barcode + manual) [2300 lines]
│   │   ├── Header.tsx        # Top app header with streak display
│   │   ├── WaterStreak.tsx   # Water intake tracker widget
│   │   ├── WeeklyReport.tsx  # Weekly nutrition/fitness summary card
│   │   └── ui/               # shadcn/ui primitives (14 components kept)
│   │       ├── badge.tsx
│   │       ├── button.tsx
│   │       ├── calendar.tsx
│   │       ├── card.tsx
│   │       ├── dialog.tsx
│   │       ├── input.tsx
│   │       ├── label.tsx
│   │       ├── popover.tsx
│   │       ├── progress.tsx
│   │       ├── radio-group.tsx
│   │       ├── select.tsx
│   │       ├── slider.tsx
│   │       ├── tabs.tsx
│   │       └── textarea.tsx
│   │
│   ├── routes/               # File-based pages (TanStack Router)
│   │   ├── __root.tsx        # Root layout, theme init, Toaster
│   │   ├── index.tsx         # Root redirect → /dashboard
│   │   ├── dashboard.tsx     # Main dashboard (calories, macros, weight chart)
│   │   ├── food.tsx          # Food log page (daily + monthly view)
│   │   ├── weight.tsx        # Weight tracking + progress photos
│   │   ├── workout.tsx       # Workout logging (gym + cardio)
│   │   ├── leaderboard.tsx   # Social leaderboard
│   │   ├── profile.tsx       # User profile, goals, plan, settings
│   │   ├── quiz.tsx          # Onboarding quiz (multi-step)
│   │   ├── plans.tsx         # Subscription plans page
│   │   ├── login.tsx         # Login / signup / forgot password
│   │   └── reset-password.tsx# Password reset
│   │
│   ├── integrations/         # Supabase client wiring
│   │   ├── client.ts         # Browser Supabase client
│   │   ├── client.server.ts  # Server-side Supabase client
│   │   ├── types.ts          # Generated Supabase Database types
│   │   ├── auth-middleware.ts # Server auth middleware (TanStack Start)
│   │   └── auth-attacher.ts  # Attaches auth to server context
│   │
│   ├── services/
│   │   └── storage.ts        # Supabase Storage service (upload/delete/replace weight photos)
│   │
│   ├── lib/
│   │   ├── auth.tsx          # Auth context / useAuth hook
│   │   ├── foodDb.ts         # Local food database + AI fallback search (calls server function)
│   │   ├── nutrition.ts      # Macro calculation helpers (BMR, TDEE, targets)
│   │   ├── error-capture.ts  # Global error capture for SSR
│   │   ├── error-page.ts     # Error page renderer
│   │   └── utils.ts          # cn() classname utility
│   │
│   ├── hooks/
│   │   └── use-mobile.tsx    # isMobile hook (window.matchMedia)
│   │
│   ├── data/
│   │   └── ifct2017.json     # IFCT 2017 Indian food nutrition database (522KB)
│   │
│   ├── routeTree.gen.ts      # Auto-generated by TanStack Router (do not edit)
│   ├── router.tsx            # Router setup
│   ├── server.ts             # SSR server entry (error handling wrapper)
│   ├── start.ts              # Client entry point
│   └── styles.css            # Global CSS + Tailwind base
│
├── supabase/
│   ├── config.toml           # Supabase CLI config
│   └── migrations/           # Ordered SQL migration files
│       ├── 20260517071940_*.sql          → initial schema (user_profiles, food_logs)
│       ├── 20260602_new_features.sql     → water_logs, weight_entries, workout tables
│       ├── 20260708_add_leaderboard_rpc.sql → get_leaderboard_stats() text overload
│       ├── 20260708_add_onboarding_fields.sql → whatsapp_no, supplements_used
│       ├── 20260708_add_saved_meals_and_streaks.sql → saved_meals, streak columns
│       ├── 20260708_add_saved_meals_rls.sql → saved_meals RLS
│       ├── 20260729152300_add_storage_rls.sql → weight-photos bucket RLS
│       ├── 20260729170000_fix_storage_select.sql → no-op guard
│       ├── 20260729_fix_goal_values.sql  → goal enum remap + macro backfill
│       ├── 20260729190000_sync_from_db.sql → get_leaderboard_stats(date) overload
│       └── 20260729190001_rename_rls_policies.sql → RLS policy name alignment
│
├── .env                      # Local secrets (not committed)
├── .env.example              # Template for required env vars
├── package.json              # Dependencies and scripts
├── tsconfig.json             # TypeScript config
├── vite.config.ts            # Vite + Nitro config (preset: "vercel")
├── eslint.config.js          # ESLint config
├── components.json           # shadcn/ui config
├── SETUP.md                  # Setup instructions
└── PROJECT_STRUCTURE.md      # This file
```

---

## Database Schema

### Tables

| Table | Purpose |
|-------|---------|
| `user_profiles` | User info, macros targets, goals, streaks |
| `food_logs` | Daily food entries per user |
| `water_logs` | Daily water intake (one row per user per day) |
| `weight_entries` | Weight log with optional progress photo |
| `workout_logs` | Workout sessions with exercises JSON |
| `workout_plans` | AI-generated workout plans (JSON) |
| `saved_meals` | User-saved meal presets |

### Functions

| Function | Description |
|----------|-------------|
| `get_leaderboard_stats(start_date text)` | Leaderboard aggregation (STABLE) |
| `get_leaderboard_stats(start_date date)` | Leaderboard aggregation (SECURITY DEFINER) |

### RLS

All tables have RLS enabled. Users can only read/write their own rows via `auth.uid() = user_id`.

### Storage

| Bucket | Access | Path format |
|--------|--------|-------------|
| `weight-photos` | Public read, authenticated write | `{user_id}/{timestamp}.{ext}` |

---

## Scripts

```bash
npm run dev      # Start local dev server (http://localhost:3000)
npm run build    # Production build (dist/ + Vercel output)
npm run preview  # Preview production build locally
npm run lint     # ESLint
npm run format   # Prettier
```

---

## Key Architectural Notes

- **SSR**: TanStack Start provides SSR. Server routes and client routes share the same file via `createServerFn`.
- **Auth**: Supabase Auth with session cookies managed by `auth-middleware.ts`. The `useAuth()` hook exposes the current user.
- **Storage**: All photo operations route through `src/services/storage.ts`. Components never call `supabase.storage` directly.
- **AI**: Groq API is called server-side via `src/server/groq.ts`, exposed through `createServerFn` in `src/server/ai.ts`. Multiple keys are round-robined for rate limit handling. Client code never touches the Groq API key.
- **Food DB**: `ifct2017.json` is the IFCT 2017 Indian food database loaded client-side for instant offline search.
- **Barcode**: ZXing library handles barcode scanning; fallback to Open Food Facts API for nutrition lookup.
- **Routing**: TanStack Router auto-generates `routeTree.gen.ts` — do not edit manually.
